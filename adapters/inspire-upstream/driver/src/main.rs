// The driver source is shared with the inspire-lianghuiqiang9 adapter: both
// benchmark the same `inspire` API, differing only in which copy of it they
// build against (crates.io =0.2.0 here, a git checkout there).
include!("../../../_lib/inspire-driver/main.rs");
