// Shared with the inspire-upstream adapter — see adapters/_lib/inspire-driver/main.rs.
// Both forks declare the same crate name, version and public API; only the copy
// of the code being measured differs.
include!("../../../_lib/inspire-driver/main.rs");
